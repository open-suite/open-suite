# 3.2 — Backend overlay lives in the fork

- Phase: 3
- Repos: open-suite/open-suite-portal + open-suite/open-suite
- Depends on: 1.5 (uv.lock fix), 3.1
- Touches: portal repo backend build; `scripts/single-vps-deploy/08-open-suite-portal.sh` (39-45)
- Conflicts with: 3.1 (`08`), 2.5 (portal Dockerfiles)

## Change

The fork owns the portal (per strategy), so the "overlay" indirection is unnecessary — build the fork's backend directly in CI (a proper `backend/Dockerfile.overlay`, or fold the 4 overlay files into the fork's normal image) and delete the heredoc in `08`.

## Verify

- `08` (or its successor) contains no inline Dockerfile.
- Portal API works on the demo.

## Status 2026-07-03 — done

The overlay lives in the fork as `backend/Dockerfile.overlay`, built and
published by the fork's publish-images workflow as
`ghcr.io/open-suite/portal-api` (portal PR #26). 08 has no inline
Dockerfile and no on-box build; portal verified live on the demo. A true
source build remains blocked on upstream uv.lock drift — noted in the
Dockerfile header.
