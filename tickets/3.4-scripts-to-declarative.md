# 3.4 — Move script mutations into patches/values

- Phase: 3 — one PR per script (this ticket is a series; each sub-item is independently parallelizable)
- Repo: open-suite/open-suite
- Depends on: 1.3 (interim `07` fix; this deletes `07`)
- Touches: `scripts/single-vps-deploy/03-restart-oidc-apps.sh`, `05-docs.sh`, `06-grist.sh`, `07-session-lifetimes.sh`, `12`'s ingress annotations; corresponding patches/values
- Conflicts with: 1.3 (`07`), 0.3/3.1 (`12`), 2.1 (realm-import patch)

## Change

- `03-restart-oidc-apps.sh`: occ SSRF/trusted_proxies → Nextcloud chart config via patch/values.
- `05-docs.sh`: y-provider svc ports → chart patch; DB grants → chart init job or documented one-time migration.
- `06-grist.sh`: replicaCount via values if the chart exposes it.
- `07-session-lifetimes.sh`: realm lifetimes → realm-import patch, then delete the script.
- `12`'s ingress annotations: patch the vendored ingress templates to carry the middleware annotation when a flag is set.

Acceptance for each: the script becomes a no-op or is deleted, and `helmfile apply` alone produces the setting.

## Verify (per item)

Run `helmfile -e demo apply` on the box after the change; the setting survives (grist replicas stay 1, session max stays bumped after config-cli re-import, docs y-provider ports correct).

## Status

- `06-grist.sh` — done. Demo values now set `autoscaling.horizontal.grist.enabled: false` (deep-merged over upstream defaults, same pattern upstream uses for collabora); the chart then pins the deployment to `replicaCount` (1). Script deleted. Not yet applied live — takes effect on next `helmfile -e demo apply` / full deploy.
