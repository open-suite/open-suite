# 1.2 — Pin the upstream infra ref

- Phase: 1
- Repo: open-suite/open-suite
- Depends on: 1.1
- Touches: `scripts/single-vps-deploy/01-deploy.sh:58`, new `UPSTREAM_REF` file
- Conflicts with: 1.1 (`01-deploy.sh`)

## Change (decided)

Clone/reset to a fixed SHA of `MinBZK/mijn-bureau-infra` recorded in a new `UPSTREAM_REF` file read by the script. Pin to the SHA currently checked out at `/root/mijn-bureau-infra` on the demo box (`git -C /root/mijn-bureau-infra rev-parse HEAD`) — the one commit the patches are known to apply to and the demo runs against.

## Verify

Fresh clone at the pinned ref + `git apply --check patches/local/*.patch` passes.
