# 4.1 — README + deploy.sh header

- Phase: 4 (after Phase 1, so docs describe the fixed state)
- Repo: open-suite/open-suite
- Depends on: Phase 1 landed (script renumbering from 1.7 settled)
- Touches: `README.md`, `deploy.sh:5` (comment header only)
- Conflicts with: 1.4 (README references), 0.1/1.3 (`deploy.sh` — header comment only, trivial rebase)

## Change

Describe the real pipeline (01-12 and what each does in one line), real result URL (`bridge.`), delete `overlays/bureaublad/` and CalDAV claims, mark the repo-shape block explicitly as target-vs-current.

## Verify

Every path and script number named in README exists on disk (`grep`-check each).
