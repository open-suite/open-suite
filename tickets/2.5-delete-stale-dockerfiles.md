# 2.5 — Delete stale fork Dockerfiles

- Phase: 2
- Repo: open-suite/open-suite-portal
- Depends on: —
- Touches: `backend/Dockerfile.local`, `backend/Dockerfile.overlay`, `frontend/Dockerfile.native` (all deleted)
- Conflicts with: 3.2 (recreates a proper backend build in the fork) — fine, this clears the ground

## Change

Delete all three (the live overlay definition is the heredoc in `08-open-suite-portal.sh`; consolidation happens in 3.2).

## Verify

`git grep -l 'Dockerfile.overlay\|Dockerfile.local\|Dockerfile.native'` in both repos returns nothing (check compose.yaml and CI too).
