# 2.6 — Fix the php-cache × meetcal opcache interaction

- Phase: 2
- Repo: open-suite/open-suite
- Depends on: —
- Touches: `patches/local/nextcloud-meetcal.patch` (occ hook), `patches/local/nextcloud-php-cache.patch`
- Conflicts with: 3.3 (supersedes this — implement the minimal fix now, note superseded-by-3.3)

## Change

Minimal fix — after the user_oidc sed, `rm -rf` the opcache dir or restart php (the hook runs in the pod; simplest reliable form is having the deploy do a `kubectl -n mb-nextcloud rollout restart` after first install, once, guarded). The better fix lives in 3.3 (move the sed to an image overlay).

## Verify

After deploy, token exchange works on first login without a manual pod restart (test: create event via meetcal endpoint returns 200 with a room URL).
