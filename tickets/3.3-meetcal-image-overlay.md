# 3.3 — meetcal as an image overlay + the user_oidc fix as a real patch

- Phase: 3
- Repo: open-suite/open-suite
- Depends on: 3.1 (registry/CI), 2.6 (interim fix this supersedes)
- Touches: `patches/local/nextcloud-meetcal.patch`, new `images/nextcloud/` build context
- Conflicts with: 2.6 (`nextcloud-meetcal.patch`)

## Change

Build `FROM nextcloud:<pinned>` adding `custom_apps/meetcal/` (PHP files as normal source files in this repo — lintable, testable) and the user_oidc TokenService one-line fix applied at build time (not runtime sed). Shrink the helm patch to: image override + occ enable + `store_login_token=1`. Add CSRF handling to the POST route (drop `#[NoCSRFRequired]`, send `requesttoken` from the header JS). Separately: submit the TokenService fix upstream to nextcloud/user_oidc and record the PR URL in the patch header.

## Verify

- `php -l` passes on the app files in CI.
- Deploy from scratch → meetcal works.
- `kubectl exec ... -- grep requested_token_type .../TokenService.php` shows the fix baked in, and no sed remains in the occ hook.
