# 2.2 — Remove the dead Element config patch; pin the bundle patch

- Phase: 2
- Repo: open-suite/open-suite
- Depends on: —
- Touches: `patches/local/element-disable-device-verification-reminder.patch`, `scripts/single-vps-deploy/11-element-web.sh`
- Conflicts with: —

## Change

First confirm the flag is dead (grep the element-web version deployed for `BulkUnverifiedSessionsReminder` handling); if dead, delete the patch. In 11, change the init container's `imagePullPolicy: Always` (line 65) to `IfNotPresent` and pin the Element image tag the perl match was written against, so drift can't break it silently.

## Verify

- Patch removed from the series and 01 still applies cleanly.
- Element loads without the verification toast on a restarted pod.
