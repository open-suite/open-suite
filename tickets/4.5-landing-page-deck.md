# 4.5 — Make the landing page true (Deck)

- Phase: 4
- Repos: open-suite/open-suite + open-suite/website
- Depends on: —
- Touches: `scripts/single-vps-deploy/08-open-suite-portal.sh` (Nextcloud occ app installs), website `index.html`
- Conflicts with: 3.1/3.2 (`08`)

## Change

Either add `occ app:install deck` (and contacts, if promoted in nav) to the deploy so a fresh install matches the landing page, or remove the Projects claim. Default: add the install — the nav already promotes Projects.

## Verify

Fresh-deploy checklist includes Deck; or claim removed.
