# 1.4 — Delete install.sh

- Phase: 1
- Repo: open-suite/open-suite
- Depends on: —
- Touches: `scripts/single-vps-deploy/install.sh` (delete), any docs referencing it
- Conflicts with: 4.1 (README) if references exist there — keep the doc edit minimal

## Change

Delete it. Grep docs for references (`grep -rn install.sh docs/ ../docs ../website README.md`) and update any.

## Verify

File gone, no dangling references.
