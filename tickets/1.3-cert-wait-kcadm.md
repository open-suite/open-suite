# 1.3 — Timeout the cert wait; check the kcadm PUT

- Phase: 1
- Repo: open-suite/open-suite
- Depends on: —
- Touches: `deploy.sh` (26-39), `scripts/single-vps-deploy/07-session-lifetimes.sh` (26-28)
- Conflicts with: 0.1 (`deploy.sh`), 3.4 (deletes `07-session-lifetimes.sh` — this is the interim fix)

## Change

- `deploy.sh:26-39`: add a max-iterations bound to the cert wait with a clear failure message.
- `07-session-lifetimes.sh:26-28`: use `curl -fsS -w '%{http_code}'`, fail non-2xx; move the admin password off the argv into `--data @-` / env-fed stdin or a netrc.

## Verify

- `bash -n` both.
- Simulate a never-ready cert (bogus domain) and confirm the wait exits nonzero within the bound.
