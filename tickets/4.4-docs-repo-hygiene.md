# 4.4 — Public docs repo hygiene

- Phase: 4 (contains exposed credentials pointers — treat as urgent within the phase)
- Repo: open-suite/docs
- Depends on: —
- Touches: `PROJECT.md`, `SESSION-TRANSCRIPT.html`, `_incoming/`, `README.md`, `it/install.md` (docs repo)
- Conflicts with: — (different repo)

## Change

Strip PROJECT.md of the root-SSH line, the password-extraction command, and agent-workflow notes (move that content into a non-public location, e.g. the distribution repo's CLAUDE.md or a private note); move SESSION-TRANSCRIPT.html and `_incoming/` out of the worktree entirely; replace README boilerplate with how the site is actually built and served; extend `it/install.md` to cover steps 10-12 and the demo-mode env vars.

## Verify

- `git ls-files | xargs grep -l '95.217.109.206'` returns nothing.
- Transcript file gone from the worktree.
