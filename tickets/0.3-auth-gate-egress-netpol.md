# 0.3 — Fix the auth-gate egress NetworkPolicy

- Phase: 0 (security)
- Repo: open-suite/open-suite
- Depends on: work on PR #27's branch (see 1.7a), together with 0.2
- Touches: `scripts/single-vps-deploy/12-auth-gate.sh` (180-187)
- Conflicts with: 0.2 (same PR branch), 3.1 (`12-auth-gate.sh`)

## Change

Add 8443 (k3s Traefik) to the allowed egress ports, matching what `02-networking.sh` does for the other namespaces. First verify on the box which path the gate actually uses to reach `id.<domain>` (`kubectl -n mb-auth-gate exec ... -- wget -O- https://id.<domain>/realms/...`) and whether the policy is enforcing.

## Verify

- Token endpoint reachable from the gate pod with the policy applied.
- Login flow works end-to-end.
