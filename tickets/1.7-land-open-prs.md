# 1.7 — Land the open PRs (decided order)

- Phase: 1
- Repo: open-suite/open-suite PRs #24, #25, #26, #27
- Depends on: 0.2 + 0.3 (applied on #27's branch), 1.2 (pin) before #25
- Touches: PR branches; `deploy.sh` sequence (renumbering)
- Conflicts with: 0.2/0.3 (part of this), 1.3/0.1 (`deploy.sh`)

## Change (decided)

(a) #27 auth gate keeps `12-auth-gate.sh`; apply tickets 0.2/0.3 on its branch, then merge.
(b) #24 header SSO links: merge as-is.
(c) #25 Meet permission patch: renumber its script `12-meet-frontend.sh` → `13-meet-frontend.sh`, update deploy.sh sequence in the PR, and hold until 1.2 lands — its source patch targets Meet v1.20.0 while upstream has bumped to v1.21.0, so verify `git apply --check` against the pinned ref before merging.
(d) #26 Meet home redesign: merge after #25 (same build pipeline).

## Verify

- No two scripts share a number on main.
- deploy.sh runs the full renumbered sequence.
- Meet patch applies at the pinned ref.
