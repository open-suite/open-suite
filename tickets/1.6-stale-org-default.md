# 1.6 — Fix the stale org default

- Phase: 1
- Repo: open-suite/open-suite
- Depends on: —
- Touches: `scripts/single-vps-deploy/08-open-suite-portal.sh:15`
- Conflicts with: 3.1, 3.2, 4.5 (all touch `08`) — this is a one-line change, land it first

## Change

`Keep-Office/open-suite-portal` → `open-suite/open-suite-portal`; also pin `PORTAL_REF` default to a tag/SHA instead of `main`.

## Verify

`grep -rn 'Keep-Office' .` returns nothing.
