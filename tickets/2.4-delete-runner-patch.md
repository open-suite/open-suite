# 2.4 — Delete the runner patch and overlay

- Phase: 2
- Repo: open-suite/open-suite
- Depends on: —
- Touches: `patches/local/runner-multi-instance.patch`, `overlays/infra/environments-runner-mijnbureau.yaml.gotmpl` (both deleted)
- Conflicts with: —

## Change (decided)

Delete both — nothing references the `runner` env, the `environments/runner/` dir it requires is never created, and it's 69% of patch payload against a moving upstream. Recoverable from git history; if multi-instance returns, the right form is upstreaming a values flag to MinBZK. Confirm 01 still applies the remaining patches cleanly.

## Verify

- `grep -rn 'runner' patches overlays scripts` returns nothing.
- Scratch-dir patch apply passes.
