# 3.6 — Assembled-stack smoke test

- Phase: 3
- Repo: open-suite/open-suite
- Depends on: 3.1 (gives CI something to deploy) for the CI wiring; manual runs against the demo need nothing
- Touches: new `ci/smoke/` only
- Conflicts with: —

## Change

A script (runnable in CI against a fresh VM, and manually against the demo) asserting: SSO login via gate works; portal loads; calendar widget returns events; `POST /apps/meetcal/room` mints a joinable room; a Doc opens; Grist opens; Element loads. Playwright or curl+token flows — smallest thing that would have caught the Bearer bypass and the helmfile-revert breakage.

## Verify

- Smoke run green against the demo box.
- Wire into CI once 3.1 gives CI something to deploy.

## Status 2026-07-03 — done (manual runs green; CI wiring open)

`ci/smoke/smoke.sh` (unauthenticated: gate closed on every host, Bearer
bypass regression, identity health, login flow reaches a rendering
Keycloak form) and `ci/smoke/authenticated.mjs` (Playwright: SSO through
the gate, portal dashboard, header injected on the sidecar apps, calendar
API, meetcal mints a joinable room, Docs/Grist/Element load). Both green
against the demo. Writing it caught four real regressions in one day:
grist/nextcloud 502 behind the from-restricted netpols, the portal
backend silently reverted to the upstream image by a helm sync, the meet
backend registry hijack, and upstream's caldav NoneType 500. CI wiring
(fresh-VM deploy + smoke in Actions) still to do.
