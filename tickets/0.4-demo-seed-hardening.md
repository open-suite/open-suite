# 0.4 — Demo seed: remove permanent client weakening and the phantom token

- Phase: 0 (security)
- Repo: open-suite/open-suite
- Depends on: —
- Touches: `scripts/demo/seed-demo.sh` (~50, ~118, ~148-149), `scripts/demo/README.md:26`
- Conflicts with: 2.3 (`seed-demo.sh`) — land this first, 2.3 rebases

## Change

(a) Re-disable `directAccessGrantsEnabled` on `meet` and `docs` clients at the end of the run (trap-based so failures still restore), or switch seeding to a proper service-account client.
(b) Replace hardcoded `syt_seedadmintokenjohn0001` with a token actually minted/bootstrapped by the script, and make the README match what the code does.
(c) Replace hardcoded `matrix.demo.opensuite.online` with `matrix.$DOMAIN`.

## Verify

- After a seed run, `kcadm get clients` shows `directAccessGrantsEnabled=false` for meet/docs.
- Seed still completes.
- `grep -c 'demo.opensuite.online' seed-demo.sh` returns 0.
