# 4.2 — Both CLAUDE.mds

- Phase: 4
- Repos: open-suite/open-suite `CLAUDE.md` + outer `/Users/g/git/ritza/open-suite/CLAUDE.md` (not in a git repo — edit in place)
- Depends on: Phase 1 landed
- Touches: the two CLAUDE.md files only
- Conflicts with: —

## Change

Domain → `demo.opensuite.online`; paths → `deploy.sh`, `scripts/single-vps-deploy/01..12`; drop the `helm/` prefix and the nonexistent `helm/CLAUDE.md` pointer; remove the superseded frontend image-patch workflow; correct the namespace list to what `kubectl get ns` shows.

## Verify

No occurrence of `ritzademo.com`, `helm/`, or `Keep-Office` in either file.
