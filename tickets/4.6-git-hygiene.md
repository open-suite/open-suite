# 4.6 — Git hygiene sweep

- Phase: 4
- Repos: open-suite/open-suite + open-suite/open-suite-portal
- Depends on: 1.7 (open-PR decisions live there)
- Touches: branches only, no files
- Conflicts with: —

## Change

Fast-forward local `main` branches; for the unpushed commits on `fix/element-no-default-e2ee` (ahead 5) and `demo-portal-combined` (ahead 6), push for record first, then delete the branches — never silent-discard; delete merged/stale branches locally and on origin.

## Verify

`git branch -vv` on both repos shows no `behind` mains and no unexplained `ahead` branches.

## Status

Done 2026-07-02. Both repos: local `main` fast-forwarded; every other local branch deleted. Unpushed work recorded first: `feat/calendar-meet-seed` pushed to origin; `fix/element-no-default-e2ee` (ahead 5, tip already in main) record-pushed then deleted with the other merged branches; portal `demo-portal-combined` (ahead 6/behind 2 — local tip in main, the 2 origin-only commits were empty merge commits of already-merged PRs #8/#10) deleted local+origin. Origin branches whose tips are ancestors of main deleted (31 on open-suite, 11 on the portal); unmerged origin branches kept as record.
