# 1.1 — Idempotent step 01

- Phase: 1
- Repo: open-suite/open-suite
- Depends on: —
- Touches: `scripts/single-vps-deploy/01-deploy.sh` (58-67)
- Conflicts with: 1.2 (same lines of `01-deploy.sh`) — land this first, 1.2 rebases

## Change

Replace bare `git clone` with clone-or-reset (`git -C dir fetch && git reset --hard <ref> && git clean -fd` if dir exists); run `git apply --check` on all patches against the clean tree before applying any; apply with `--3way`.

## Verify

Run the script body's clone+patch section twice in a row in a scratch dir; second run succeeds.
