# 2.1 — One owner for Keycloak branding + theme

- Phase: 2
- Repo: open-suite/open-suite
- Depends on: 0.1 (also edits `10-keycloak-login.sh`) — land 0.1 first
- Touches: `patches/local/keycloak-realm-open-suite-branding.patch`, `scripts/single-vps-deploy/10-keycloak-login.sh` (277-280)
- Conflicts with: 0.1 (`10-keycloak-login.sh`)

## Change

Add `loginTheme: opensuite` to the realm-import patch; delete the script's kcadm displayName/displayNameHtml/loginTheme calls (keep the theme-configmap + volumeMount parts of 10, which are legitimately deploy-side).

## Verify

After `helmfile apply` + config-cli re-import on a test run, login page shows the Open Suite theme without running 10's kcadm section.
