# 1.5 — Merge the portal CI fix

- Phase: 1
- Repo: open-suite/open-suite-portal (branch `fix/portal-ci-cleanup`, `d43d304`)
- Depends on: —
- Touches: portal repo only
- Conflicts with: — (different repo)

## Change

Open/merge the PR to main. PR #20 shows as merged in the PR list but `origin/main` at `ba04545` predates it — check `gh pr view 20 -R open-suite/open-suite-portal --json state,mergedAt` and `git log origin/main` after fetch; if actually merged, just note it and move on.

## Verify

On main, `frontend-ci.yaml` PR filter watches `frontend/**` and no workflow pushes to `ghcr.io/minbzk/*`.
