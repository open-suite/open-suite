diff --git a/src/frontend/src/features/rooms/livekit/components/blur/BackgroundCustomProcessor.ts b/src/frontend/src/features/rooms/livekit/components/blur/BackgroundCustomProcessor.ts
index d13b047..4b4f255 100644
--- a/src/frontend/src/features/rooms/livekit/components/blur/BackgroundCustomProcessor.ts
+++ b/src/frontend/src/features/rooms/livekit/components/blur/BackgroundCustomProcessor.ts
@@ -1,7 +1,6 @@
 import type { ProcessorOptions, Track } from 'livekit-client'
 import posthog from 'posthog-js'
-import {
-  FilesetResolver,
+import type {
   ImageSegmenter,
   ImageSegmenterResult,
 } from '@mediapipe/tasks-vision'
@@ -79,6 +78,10 @@ export class BackgroundCustomProcessor implements BackgroundProcessorInterface {
       throw new Error('Element is required for processing')
     }
 
+    const { FilesetResolver, ImageSegmenter } = await import(
+      '@mediapipe/tasks-vision'
+    )
+
     this.source = opts.track as MediaStreamTrack
     this.sourceSettings = this.source!.getSettings()
     this.videoElement = opts.element as HTMLVideoElement
@@ -95,7 +98,7 @@ export class BackgroundCustomProcessor implements BackgroundProcessorInterface {
     this.processedTrack = tracks[0]

     this.segmentationMask = new ImageData(PROCESSING_WIDTH, PROCESSING_HEIGHT)
-    await this.initSegmenter()
+    await this.initSegmenter(FilesetResolver, ImageSegmenter)
     this._initWorker()

     posthog.capture('firefox-blurring-init')
@@ -152,7 +155,10 @@ export class BackgroundCustomProcessor implements BackgroundProcessorInterface {
     }
   }

-  async initSegmenter() {
+  async initSegmenter(
+    FilesetResolver: typeof import('@mediapipe/tasks-vision').FilesetResolver,
+    ImageSegmenter: typeof import('@mediapipe/tasks-vision').ImageSegmenter
+  ) {
     const vision = await FilesetResolver.forVisionTasks(
       'https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision/wasm'
     )
diff --git a/src/frontend/src/features/rooms/livekit/components/blur/FaceLandmarksProcessor.ts b/src/frontend/src/features/rooms/livekit/components/blur/FaceLandmarksProcessor.ts
index 7299ae4..561d919 100644
--- a/src/frontend/src/features/rooms/livekit/components/blur/FaceLandmarksProcessor.ts
+++ b/src/frontend/src/features/rooms/livekit/components/blur/FaceLandmarksProcessor.ts
@@ -1,7 +1,6 @@
 import type { ProcessorOptions, Track, TrackProcessor } from 'livekit-client'
 import posthog from 'posthog-js'
-import {
-  FilesetResolver,
+import type {
   FaceLandmarker,
   FaceLandmarkerResult,
 } from '@mediapipe/tasks-vision'
@@ -55,7 +54,6 @@ export class FaceLandmarksProcessor implements TrackProcessor<Track.Kind> {
     this.name = 'face_landmarks'
     this.options = opts
     this.type = ProcessorType.FACE_LANDMARKS
-    this._initEffectImages()
   }
 
   private _initEffectImages() {
@@ -81,10 +79,15 @@ export class FaceLandmarksProcessor implements TrackProcessor<Track.Kind> {
       throw new Error('Element is required for processing')
     }

+    const { FilesetResolver, FaceLandmarker } = await import(
+      '@mediapipe/tasks-vision'
+    )
+
     this.source = opts.track as MediaStreamTrack
     this.sourceSettings = this.source!.getSettings()
     this.videoElement = opts.element as HTMLVideoElement

+    this._initEffectImages()
     this._createMainCanvas()

     const stream = this.outputCanvas!.captureStream()
@@ -94,7 +97,7 @@ export class FaceLandmarksProcessor implements TrackProcessor<Track.Kind> {
     }
     this.processedTrack = tracks[0]

-    await this.initFaceLandmarker()
+    await this.initFaceLandmarker(FilesetResolver, FaceLandmarker)
     this._initWorker()

     posthog.capture('face-landmarks-init')
@@ -127,7 +130,10 @@ export class FaceLandmarksProcessor implements TrackProcessor<Track.Kind> {
     }
   }

-  async initFaceLandmarker() {
+  async initFaceLandmarker(
+    FilesetResolver: typeof import('@mediapipe/tasks-vision').FilesetResolver,
+    FaceLandmarker: typeof import('@mediapipe/tasks-vision').FaceLandmarker
+  ) {
     const vision = await FilesetResolver.forVisionTasks(
       'https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision/wasm'
     )
diff --git a/src/frontend/src/features/rooms/livekit/components/blur/OpenSuiteBackgroundProcessor.ts b/src/frontend/src/features/rooms/livekit/components/blur/OpenSuiteBackgroundProcessor.ts
index eeef587..a5d718e 100644
--- a/src/frontend/src/features/rooms/livekit/components/blur/OpenSuiteBackgroundProcessor.ts
+++ b/src/frontend/src/features/rooms/livekit/components/blur/OpenSuiteBackgroundProcessor.ts
@@ -1,6 +1,5 @@
 import type { ProcessorOptions, Track } from 'livekit-client'
-import {
-  FilesetResolver,
+import type {
   ImageSegmenter,
   ImageSegmenterResult,
 } from '@mediapipe/tasks-vision'
@@ -193,6 +192,10 @@ export class OpenSuiteBackgroundProcessor
 
   async init(opts: ProcessorOptions<Track.Kind>) {
     if (!opts.element) throw new Error('Element is required for processing')
+    const { FilesetResolver, ImageSegmenter } = await import(
+      '@mediapipe/tasks-vision'
+    )
+
     this.source = opts.track as MediaStreamTrack
     this.videoElement = opts.element as HTMLVideoElement

diff --git a/src/frontend/src/features/rooms/livekit/components/blur/index.ts b/src/frontend/src/features/rooms/livekit/components/blur/index.ts
index e0123db..7edbf63 100644
--- a/src/frontend/src/features/rooms/livekit/components/blur/index.ts
+++ b/src/frontend/src/features/rooms/livekit/components/blur/index.ts
@@ -1,9 +1,7 @@
-import { ProcessorWrapper } from '@livekit/track-processors'
-import type { Track, TrackProcessor } from 'livekit-client'
+import type { ProcessorOptions, Track, TrackProcessor } from 'livekit-client'
 import { BackgroundCustomProcessor } from './BackgroundCustomProcessor'
 import { OpenSuiteBackgroundProcessor } from './OpenSuiteBackgroundProcessor'
-import { UnifiedBackgroundTrackProcessor } from './UnifiedBackgroundTrackProcessor'
-import { FaceLandmarksOptions } from './FaceLandmarksProcessor'
+import type { FaceLandmarksOptions } from './FaceLandmarksProcessor'
 
 export enum ProcessorType {
   BLUR = 'blur',
@@ -21,15 +19,71 @@ export interface BackgroundProcessorInterface extends TrackProcessor<Track.Kind>
   options: ProcessorConfig
 }
 
+const hasModernProcessorSupport = () =>
+  'MediaStreamTrackGenerator' in globalThis &&
+  'MediaStreamTrackProcessor' in globalThis
+
+const hasProcessorSupport = () =>
+  hasModernProcessorSupport() ||
+  (typeof HTMLCanvasElement !== 'undefined' &&
+    typeof VideoFrame !== 'undefined' &&
+    'captureStream' in HTMLCanvasElement.prototype)
+
+class LazyUnifiedBackgroundTrackProcessor
+  implements BackgroundProcessorInterface
+{
+  options: ProcessorConfig
+  name: string
+  private processor?: BackgroundProcessorInterface
+
+  constructor(options: ProcessorConfig) {
+    this.options = options
+    this.name =
+      options.type === ProcessorType.VIRTUAL
+        ? 'virtual-background'
+        : 'background-blur'
+  }
+
+  get processedTrack() {
+    return this.processor?.processedTrack
+  }
+
+  async init(opts: ProcessorOptions<Track.Kind>) {
+    const { UnifiedBackgroundTrackProcessor } = await import(
+      './UnifiedBackgroundTrackProcessor'
+    )
+    this.processor = new UnifiedBackgroundTrackProcessor(this.options)
+    await this.processor.init(opts)
+  }
+
+  async restart(opts: ProcessorOptions<Track.Kind>) {
+    if (!this.processor) return this.init(opts)
+    await this.processor.restart(opts)
+  }
+
+  async destroy() {
+    await this.processor?.destroy()
+  }
+
+  async update(opts: ProcessorConfig) {
+    this.options = opts
+    this.name =
+      opts.type === ProcessorType.VIRTUAL
+        ? 'virtual-background'
+        : 'background-blur'
+    await this.processor?.update(opts)
+  }
+}
+
 export class BackgroundProcessorFactory {
   static hasModernApiSupport() {
-    return ProcessorWrapper.hasModernApiSupport
+    return hasModernProcessorSupport()
   }
 
   static isSupported() {
     return (
       OpenSuiteBackgroundProcessor.isSupported ||
-      ProcessorWrapper.isSupported ||
+      hasProcessorSupport() ||
       BackgroundCustomProcessor.isSupported
     )
   }
@@ -48,8 +102,8 @@ export class BackgroundProcessorFactory {
       return new OpenSuiteBackgroundProcessor(config)
     }
 
-    if (ProcessorWrapper.isSupported) {
-      return new UnifiedBackgroundTrackProcessor(config)
+    if (hasProcessorSupport()) {
+      return new LazyUnifiedBackgroundTrackProcessor(config)
     }
 
     if (BackgroundCustomProcessor.isSupported) {
diff --git a/src/frontend/vite.config.ts b/src/frontend/vite.config.ts
index f58ef7b..efe5f67 100644
--- a/src/frontend/vite.config.ts
+++ b/src/frontend/vite.config.ts
@@ -26,6 +26,19 @@ export default defineConfig(({ mode }) => {
     },
     build: {
       sourcemap: env.VITE_BUILD_SOURCEMAP === 'true',
+      modulePreload: {
+        // Keep optional background processors out of the room route's
+        // recursive preloads. Their own dynamic imports retain all preloads.
+        resolveDependencies: (filename, deps) =>
+          /vision_bundle|UnifiedBackgroundTrackProcessor/.test(filename)
+            ? deps
+            : deps.filter(
+                (dependency) =>
+                  !/vision_bundle|UnifiedBackgroundTrackProcessor/.test(
+                    dependency
+                  )
+              ),
+      },
     },
     server: {
       port: parseInt(env.VITE_PORT) || 3000,
